#!/bin/sh
for arg in $@; do
    case $arg in
        '--nobuild') nobuild=1 ;;
        '--help') help=1 ;;
    esac
done
if [ ! $nobuild ] && [ ! $help ]; then
    env ./node_modules/.bin/cake build || exit 1
fi
exec env node lib/main.js $@

